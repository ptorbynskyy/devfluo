<project-context>
## Current Project Knowledge Context

<architcture>
### Architecture
<%= it.context.architecture %>
</architcture>
<codebase>
### Codebase
<%= it.context.codebase %>
</codebase>
<memory-cards>
<% if (it.context.memoryCards.length > 0) {%>
### Memory Context
<% it.context.memoryCards.forEach(card => {%>
<%~ include('memory-card', { card }) -%>
<% }) %>
<% } %>
</memory-cards>
</project-context>